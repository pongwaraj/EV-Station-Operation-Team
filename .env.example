# Neon PostgreSQL. Keep real values in local/Vercel environment variables only.
DATABASE_URL=

# Private object storage token for raw Excel/CSV uploads.
BLOB_READ_WRITE_TOKEN=

# Authentication secret. Generate a unique value per environment.
AUTH_SECRET=

# Public application URL for redirects and links.
NEXT_PUBLIC_APP_URL=http://localhost:3000
